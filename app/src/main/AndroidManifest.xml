<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Read incoming UPI payment notifications -->
    <!-- (BIND_NOTIFICATION_LISTENER_SERVICE is declared on the service itself) -->

    <!-- Draw the payment prompt over other apps + launch it from the background -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Read contacts for the split picker -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- POST rows to the Google Apps Script endpoint -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Post a heads-up notification as a fallback trigger -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed to resolve/launch WhatsApp via package name on Android 11+ -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SplitMate">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.DebugLogActivity"
            android:exported="false"
            android:label="@string/debug_title" />

        <!-- The pop-up shown after a payment. Transparent, appears over the UPI app. -->
        <activity
            android:name=".ui.PaymentPromptActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.SplitMate.Transparent" />

        <!-- Notification listener that detects UPI payments -->
        <service
            android:name=".service.PaymentNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
